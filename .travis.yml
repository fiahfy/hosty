os: osx
osx_image: xcode9.4

language: node_js
node_js: 8

cache:
  yarn: true
  directories:
    - node_modules
    - $HOME/Library/Caches/electron
    - $HOME/Library/Caches/electron-builder

script:
  - yarn test
  - yarn release

branches:
  only:
    - master
