os: osx
osx_image: xcode9.2

language: node_js
node_js: 8

cache:
  yarn: true
  directories:
  - node_modules
  - $HOME/.cache/electron
  - $HOME/.cache/electron-builder

install:
  - yarn

script:
  - yarn test
  - yarn release

branches:
  only:
    - master
